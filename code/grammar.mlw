module Grammar

  use option.Option
  use fmap.MapImpInt as M

  type id = int
  type state = id -> option int

  type aexpr =
       | Acst int
       | Avar id
       | Asub aexpr aexpr

  type bexpr =
       | Btrue
       | Bfalse
       | Bleq aexpr aexpr

  type stmt =
       | Sskip
       | Sass id aexpr
       | Sseq stmt stmt

  type prog = stmt

end
